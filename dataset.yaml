path: E:/Smart/dataset  # Root dataset directory
train: train/images  # Training images folder (relative to 'path')
val: valid/images  # Validation images folder (relative to 'path')
test: test/images  # (optional) Test images folder

nc: 3  # Number of classes (update this based on your dataset)
names: ["spaces", "space-empty", "space-occupied"]  # Class names
